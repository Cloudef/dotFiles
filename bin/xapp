#!/bin/sh
# Start application in seperate X session

if [ ! "$1" ]; then
	echo "Usage : xapp [program]"
	exit
fi

type -P "$1" &>/dev/null || { echo "$1 : not found." >&2; exit 1; }

X :2 -ac -terminate & nvidia-settings --load-config-only
sleep 10
DISPLAY=:2
$@