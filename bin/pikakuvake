#!/bin/sh
if [[ ! "$1" ]]; then
   echo "Usage: pikakuvake <application>"
   exit
fi

DIR=`dirname  "$1"`
BIN=`basename "$1"`
shift; cd "$DIR"
export WINEDEBUG=-all
if [ ! `wine "$BIN" "$@"` ]
then
	./"$BIN" \"$@\"
fi
