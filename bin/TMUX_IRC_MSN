#!/bin/sh
# Start IRC && MSN in tmux

# Main
main()
{
   tmux new-session -s 'IRC_MSN' -n 'IRC' -d 'irssi'
   tmux new-window -n 'MSN' "irssi --config=$HOME/.irssi/bitlbee"
   tmux -2 attach-session -d -t 'IRC_MSN'
}

# Go
tmux attach-session -t 'IRC_MSN' -d
[ "$?" -eq 0 ] || main
